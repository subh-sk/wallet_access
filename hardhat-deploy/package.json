{
  "name": "usdt-deploy",
  "version": "1.0.0",
  "description": "Deploy USDT contracts to BSC",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}

